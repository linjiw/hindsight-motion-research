{
  "schema": "hindsight_matched_continuation_v1",
  "authorization": "User requested continuing research according to the updated plan",
  "scope": "Development-only unperturbed same-phase reference handoff; first subset of matrix row 2",
  "parent_packet": "runs/complete_task_20260918_v1",
  "source_clips": ["00976", "00265"],
  "conditions": ["beam"],
  "methods": ["continuous", "linear29"],
  "states": ["pre_entry", "entry", "exit", "pre_hold"],
  "state_selection": {
    "pre_entry": "max(1, parent continuous entry_tick - 15)",
    "entry": "parent continuous entry_tick + 1",
    "exit": "parent continuous clear_tick + 1",
    "pre_hold": "parent continuous control_steps - hold_ticks - 10"
  },
  "timing": "Handoff k is the observation before action k, after k common-prefix physics steps; parent scorer ticks are zero based post-step indices",
  "maximum_native_attempts": 16,
  "maximum_control_steps_per_attempt": 500,
  "maximum_control_steps_total": 8000,
  "maximum_physics_steps_total": 32000,
  "maximum_wall_seconds_per_attempt": 600,
  "resource_wait_seconds_per_attempt": 300,
  "seed": 96161,
  "retries": 0,
  "training_steps": 0,
  "historical_main_budget_consumed": 0,
  "task_profile": "whole_body_passage_upright_recovery_goal050_stop_v1",
  "handoff": "Same reference cursor; replace loaded native reference tensors after prefix action k-1 is computed and before its physics/next observation; never reset physical state/history",
  "extra_handoff_anchors": 0,
  "blend_frames": 0,
  "linear29": "Reuse exactly the frozen full-reference Linear29 decode, including its existing wrist clipping; initial two reset frames precede all handoffs",
  "assistance": "Full continuous prefix, prescribed reference phase, original full root and complete future continuation remain supplied and charged",
  "matching_tolerance": 0.00001,
  "adapter_tolerance": 0.00001,
  "gate": "Each continuous control must match the parent prefix and complete the task before its Linear29 pair; each executed pair must match full prefix and handoff state/history/RNG or stop",
  "stopping_rules": "Stop on infrastructure failure, adapter mismatch, continuous qualification failure, paired state/history/RNG mismatch, or resource timeout; retain every failure and all unrun cells",
  "ancestry": "Two authored development clips, broader independence unresolved; states within a clip are correlated",
  "not_tested": ["perturbation recovery", "new motion selection", "cross-phase chunk composition", "causal root planning", "autonomous navigation", "held-out generalization"]
}
