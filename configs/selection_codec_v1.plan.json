{
  "schema": "hindsight_selection_codec_pilot_v1",
  "authorization": "User requested continuing the research plan after matched-state qualification",
  "scope": "Familiar known-map from-reset controller; fixed continuous planner, continuous versus Linear29 motor reference",
  "parent_packet": "/home/linjiw/research-data/m2s-whole-motion-selection-20260918-v2",
  "methods": ["continuous", "linear29"],
  "conditions": ["clear", "shift_earlier_100mm"],
  "maximum_native_attempts": 4,
  "maximum_control_steps_per_attempt": 500,
  "maximum_control_steps_total": 2000,
  "maximum_physics_steps_total": 8000,
  "maximum_wall_seconds_per_attempt": 600,
  "resource_wait_seconds_per_attempt": 300,
  "retries": 0,
  "training_steps": 0,
  "historical_main_budget_consumed": 0,
  "source_ancestries": ["00976"],
  "candidates": ["nominal", "local_duck"],
  "linear29": {
    "knot_offset": 2,
    "knot_stride": 5,
    "bits": 12,
    "reset_anchor_frames": 2,
    "scale": "Frozen clearance_tokens_20260915_v1 scale, converted by joint name",
    "qdot": "Native forward difference at 50Hz; final velocity repeats penultimate difference",
    "clock": "Encode the 499-frame library on its source execution grid; gather true committed source indices after selection/retiming",
    "root_orientation": "Unchanged clean planned rotations in both methods"
  },
  "planner": "Unmodified sibling PublicMotionSelector over continuous bank in both arms; each arm then visits its own physical states",
  "controls": "Same per-context seed, reset, dynamics, motor, observation history, map, goal, task scorer; original sibling continuous replay must match before interpreting codec outcomes",
  "stopping": "Offline continuous reference and committed-index identity before launch; continuous complete success and exact parent parity before paired codec launch; unmatched initial state/history, infrastructure failure or resource timeout stops queue; keep behavioral codec failures",
  "cost_boundary": "Planner still holds full continuous library, bounds and support; this is a motor-reference representation intervention, not end-to-end memory or bitrate savings",
  "limitations": "One familiar source ancestry, two inherited contexts, known map and simulator localization; no heldout variation, family stitching, physical LLM use or student fitting"
}
