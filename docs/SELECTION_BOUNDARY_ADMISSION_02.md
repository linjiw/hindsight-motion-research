# Boundary comparison: admission 02

September 19, 2026. The user requested continuation after the [partial six-case study](SELECTION_BOUNDARY_RESULTS_zh.md). This admission implements its saved next step; the [scientific protocol](SELECTION_BOUNDARY_PROTOCOL.md) remains frozen.

The original packet is closed with two completed shifted-beam runs and four never-launched cases. Preserve its registration, plan, results, wait ledger and timeout. The successor `runs/selection_boundary_20260919_admission02/` contains only lower100 continuous/Linear29 followed by farther600 continuous/Linear29. No shifted-beam replay, retry, new scene selection, source change, codec fitting or controller modification is authorized by this admission.

Before preparation, verify the pinned parent registration/aggregate and every bound file; reconcile actual launch/exit receipts against its two completed and four unrun rows. Reject any attempted case from the successor, any stale parent summary, or any increase beyond six native attempts across both admissions. Tasks remain byte-identical; config/command changes only relocate output/config/task paths. Copy the prior offline parent replay, and bind all successor inputs and the admission code before execution.

Maximum **four new attempts**, 500 control steps each, 2,000 new control steps / 8,000 physics samples, 600 seconds per native process, the existing serial 300-second resource wait per case, no retry or training. The original study's six-attempt / 3,000-step ceiling still applies across admissions. Two shifted-beam executions already consumed 719 control steps. This is one new admission, not permission to keep renewing resource waits.

Reuse the frozen native callback, runner, checkpoints, bank, decoded Linear29 data, seed, task scorer and exact physical parent/pair qualification. A reproduced continuous contact or deadline failure is an informative control and does not prevent the paired Linear29 run. Resource expiry, infrastructure failure or mismatched controls stops the queue. Recompute physical scores and reference/index replay; retain all registered outcomes. The new aggregate links the two old rows to their original receipts and the four new rows to this admission. Distinguish new and cumulative cost; do not rewrite the old partial aggregate or count reused runs twice.

The neighboring repository has since reached `8b90ccaa`, with a separately qualified distance-exit controller. Its new reference blending, one-time decision and fixed composed clock are a different controller package. This admission deliberately finishes the frozen old-selector comparison. The new controller's eight runs stay in its own ledger; subsequent codec work requires its own interface/transition audit and registration.
