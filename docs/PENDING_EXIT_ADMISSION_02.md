# Pending exit: one-case resource admission 02

Registered 2026-09-20, before execution, under the user's request to continue the saved research plan. This follows the [partial result](PENDING_EXIT_RESULTS_zh.md) and preserves the [scientific protocol](PENDING_EXIT_PROTOCOL.md). The [machine registration](../configs/pending_exit_admission_02.plan.json) binds the closed parent and admits only `farther-beam_linear29` in a new directory. No failed or completed native case is repeated.

The first queue completed three successful controls exactly matching their incumbent trajectories, then received SIGTERM during the fourth case's memory wait. Sender remains unknown. Preserve its 143 exit observation, stopped receipt, all 15 memory samples and lack of a fourth launch; do not reclassify it as a normal resource timeout or native failure.

## Frozen comparison and limits

Reuse all three qualified controls and the four older incumbent cases by hash. Reuse the completed farther continuous episode for exact initial state/history/dynamics/RNG and initial family/cost pairing. Bind all 363 parent inputs, the three completed native histories and qualification receipts, parent registration/plan/aggregate, original commitment proof, offline replay, interruption receipts and this new admission source/registration/protocol. New task bytes must match the never-launched task; only output/config/command paths change.

Allow **one new native attempt, at most 500 control ticks / 2,000 physics samples / 600 native wall seconds**, zero retries/training/teacher-action queries. Across admissions the original four-attempt ceiling remains unchanged: three already consumed. Resource admission still needs GPU ≥12,000 MiB and host ≥16,384 MiB twice consecutively, 20 seconds apart, within 300 seconds. Resource deferral, infrastructure failure or qualification mismatch closes this admission. Never rerun a packet's execution entrypoint.

Keep the original endpoint predictor, Linear29 decoder, root/support arrays, motor/normalization, scene/task/seed/reset, 2 cm measured gate, inclusive tick-162 deadline and five-control-tick commitment. Sibling commit `2a3cfb47` reports a successful continuous pending exit for a backward reset with calibrated endpoint predictions and latest tick 167. It is separate evidence: do not import its predictor/window, supplied-loop treatment, retry policy or success into our codec comparison.

## Analysis and decisions

Independently reconstruct every issued reference/index/lifecycle and rescore 200 Hz contacts and complete-task outcomes. Require paired entry/RNG against the reused farther continuous. Compare the new run with incumbent farther Linear29 and the retained original-goal Linear29. No reference/token/action divergence may precede actual acceptance; measured pre-action divergence must be later than acceptance. Verify all dense committed fields and unpack the native reference with its official layout before assigning forecast time offsets. Preserve the original tick-126 request separately from eventual acceptance and execution.

Report passage, upright recovery, fresh 50-tick hold, unchanged 0.50 m **3D** radius and 0.10 m/s speed threshold, falls/contacts and deadline. Success after acceptance supports a temporal-interface repair only in this selected development task. Admission followed by failure redirects work to decoded-loop continuation or stopping support. No legal admission preserves unsupported. A resource deferral leaves the scientific question open. Do not infer generalization, isolated quantization causality, compression benefit or learned improvement.

Keep old partial public results unchanged; publish a separate aggregate merging reused controls and this one new case, with separate new/cumulative costs. Any follow-up learning or execution requires its own scoped registration.
