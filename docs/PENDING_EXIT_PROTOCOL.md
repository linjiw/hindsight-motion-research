# Bounded pending-exit comparison

Registered 2026-09-20, before new native execution. The user requested continuation of the saved [temporal-admission plan](CLEARANCE_ADMISSION_DIAGNOSTIC_zh.md). [Machine registration](../configs/pending_exit_v1.plan.json). Four new cases; preserve the four [incumbent cases](DISTANCE_CODEC_ADMISSION02_RESULTS_zh.md), their failure and exhausted budget.

## Question and treatment

Does retaining a clearance-blocked distance request within a proven commitment window preserve the farther-goal Linear29 task, while retaining the three previously successful paths? The competing explanation is that accepting the later decoded loop changes the motor forecast/history and still fails passage, recovery or stopping. Later clearance by itself is not transition qualification.

Keep the original endpoint predictor; do not substitute the sibling's executed-endpoint calibration. At tick 126, run the original distance decision exactly once. A short request stays short. A loop with an open gate follows the original loop. A loop blocked by the measured full-body 2 cm clearance gate becomes pending. Recheck that same measured gate each control tick through the proven deadline; accept at its first legal opening. Freeze the requested route and tick-126 predictions instead of adding a tick-126 displacement estimate to a later root. At tick 163, an unaccepted request expires and stays short. Expiration/unsupported is not success or a qualified safe stop.

The all-family array audit finds the first continuous branch difference at frame 172, decoded q at 168, decoded qdot at 167. The short route’s implicit direct-source index/family and non-synthetic status are compared with the loop’s explicit donor/bridge metadata without changing either artifact. All dense root, bounds, support proposal/observation/validity and other bank fields are checked too, with exact joint schema and 0.02 s clock. Five committed frames require `tick + 4 < 167`, hence **common latest acceptance tick 162**, inclusive, for both methods. Derive this again from the bound arrays in preparation and check the selected world-aligned pair at runtime. Do not extend the window using padding.

This deliberately versions the temporal interface: the next five **control ticks** (0.10 s) are committed; farther samples of the ten-sample, 100 ms-spaced motor horizon may change. A later choice may revise forecasts previously visible to the motor. Current joint equality does not imply action equality. Retain source/control/reference clocks, root/velocity/rotations and synthetic masks; do not reset progress, splice arbitrary frames or rewrite actual history. No claim of preserving the old full-preview timing property.

## Matrix, frozen controls and qualification

Run original-beam continuous, original-beam Linear29, farther-beam continuous, farther-beam Linear29, in this order. Reuse the four incumbent runs by hash; all four pending cells are new native executions. Exact task bytes, scene, goal, reset, decoder, motor, normalization, original continuous planning bank, Linear29 scales/reset anchors/velocity rule, root, support, seed 96161 and scorer remain fixed. No forced family/count or teacher future/action. One ancestry 00976 and a familiar reset; selected development cases, not a population sample.

Before launching, disable the wrapper and replay every one of the incumbent 1,660 states/references/indices/decisions exactly. Enabled shadows must retain both continuous histories and original-goal Linear29. On the actual incumbent farther-Linear29 history, record the predicted acceptance and first changed forecast separately; this is not an executed continuation. Unit checks cover early/on-time/late/no clearance, deadline inclusivity, locked requests, expiry, clock gaps, mismatched fields and nonfinite data.

The serial resource gate requires two qualifying samples 20 s apart, at least 12,000 MiB GPU and 16,384 MiB host, with at most 300 s waiting per case. New budget: **four attempts, at most 2,000 control ticks / 8,000 physics samples**, at most 500 ticks and 600 native wall seconds each, zero retries/training/teacher-action queries. Do not consume the historical main balance. A failure remains evidence; resource timeout or infrastructure/qualification failure closes the queue. Only fresh registration can supersede a closed packet.

Before continuing after each case, continuous must reproduce its incumbent arrays and original decision exactly. The original-goal Linear29 must also retain its full incumbent trajectory. Both method pairs must match reset/history/dynamics/RNG and initial family costs exactly. Bind scientific registration, this protocol, new source snapshot, old input/source hashes, incumbent receipts/raw records, offline audit, commitment proof and all launch/config/task paths before execution. Keep the sibling dirty workspace unchanged; its reset study is now committed at 9e7d5c3e but supplies no new controller to this comparison.

## Outcomes and interpretation

Use the frozen complete-task scorer: passage, recovery, fresh 50-tick hold, **3D** root-goal distance ≤0.50 m, speed ≤0.10 m/s, no forbidden contact/fall and the 500-tick deadline. Preserve the [distance-dimension correction](DISTANCE_METRIC_CORRECTION_20260919.md); prediction error remains XY. Save all actions, pre/post states and 200 Hz pair-resolved contacts locally. Log original request and selected route separately, every pending gate check, accepted/expired time and actual completion. The legacy `distance-exit.json` decision stays the original tick-126 decision; `pending-exit.json` and per-tick labels supply the later actual route.

Independently rescore every completed history and rebuild each issued reference/index. Check the admission schedule from recorded measured states independently of the request state machine, and compare complete lifecycle records. Compare the new and old goal histories and the new versus incumbent traces; no divergence may precede the actual admitted request. Report complete outcomes, all failures, goal/speed/contact margins and costs separately from reused runs. Preserve three exact-retention controls and report the fourth as a changed physical path.

If only farther-Linear29 changes from deadline to complete success, this supports the bounded temporal interface as an operative repair in this task. If admission occurs but the task still fails, locate the failed phase and qualify decoded-loop support/codec components separately. If no legal admission occurs, retain unsupported and acquire a compatible continuation. A continuous or original-goal regression invalidates retention and stops this qualification. Do not infer generalization, statistical equivalence, isolated quantization causality, compression gain or language grounding.
