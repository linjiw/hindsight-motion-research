# Distance-codec resource admission 02

2026-09-19. The user requested continuation after the registered four-case experiment was deferred without a native launch. This admission executes only its four never-launched cases. [Scientific protocol](DISTANCE_CODEC_PROTOCOL.md), [scientific registration](../configs/distance_codec_v1.plan.json), [admission registration](../configs/distance_codec_admission_02.plan.json).

The original packet `runs/distance_codec_20260919_v1/` is closed: zero attempts, four unrun cases, a 300-second resource timeout, exact 792-state continuous replay, exact decoded arrays and an independent packing check. Bind its registration, plan, aggregate, execution-start, resource ledger/deferral and packing receipt; retain them unchanged. Require all 251 original input/source bindings and exact ordered case identities before preparing or launching the successor.

Copy the original scientific registration and exact task bytes. Change only run/output paths in config/command. Preserve seed, reset/history, dynamics, planner, full continuous bank, root, support masks, decoder, motor and scorer. Reuse the frozen `distance_study.run`, `distance_study.qualify`, `distance_report.audit_case`, `distance_report.replay` and `distance_report.goal_contrast`; report results to a new public aggregate instead of overwriting the original zero-launch summary.

**Budget across both admissions: four native attempts, at most 2,000 control steps / 8,000 physics samples.** Prior consumption is zero. Retain at most 500 control ticks and 600 wall seconds per attempt, a 300-second resource wait with two ready samples 20 seconds apart (≥12,000 MiB GPU and ≥16,384 MiB host), zero retries and zero training. Same case order: original continuous/Linear29, farther continuous/Linear29. Stop on resource timeout, infrastructure failure or qualification mismatch. Reproduced behavioral failure remains a valid continuous control and is not censored from the comparison.

The neighboring project is now at `eb8f1f8c`, adding a separate executed-endpoint model and wrapper. Its 12/12 calibrated versus 11/12 incumbent result does not change any of our frozen source files or authorize replacing our predictor. This admission does not claim to test the calibrated controller. The narrow stopping margins in that new study inform the next research question after the present four cases are resolved.

Save this protocol, admission source and registration before execution. Analyze actual states and complete task outcomes under the original protocol, preserving its declared velocity-convention and forecast differences. Keep generated references, replayed records and measured native simulation outcomes distinct; publish only scalar summaries, hashes and figures. A single source ancestry and seed cannot establish generalization or equivalence.
